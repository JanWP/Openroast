"""Hardware driver implementations for localroaster.

Create `localroaster/drivers/default.py` with a `create_driver(config)` factory
that returns a `localroaster.controller.HardwareDriver` implementation to make
`localroaster.create_controller()` use real hardware instead of the mock.
"""

